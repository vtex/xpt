{
  "name": "xjtty",
  "version": "1.0.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "prepare": "yarn lint && yarn test && yarn build",
    "build": "tsc",
    "test": "jest",
    "lint": "tsc --noEmit --pretty && tslint -c tslint.json './**/*.ts' -e './lib/**/*'"
  },
  "files": [
    "lib"
  ],
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "testRegex": "^.+\\.spec\\.ts$"
  },
  "repository": "git@github.com:vtex/xjtty",
  "license": "MIT",
  "dependencies": {
    "ramda": "^0.26.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "@types/node": "^10.12.9",
    "@types/ramda": "^0.25.42",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "tslint-config-vtex": "^2.0.0",
    "typescript": "^3.1.6"
  }
}
